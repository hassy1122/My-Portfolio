/* portfolio public snapshot — visitors load this file.
   Replace it via admin panel → CV tab → "Export site-data.js (publish)".
   Guard: do not wipe data injected before this script runs. */
window.SITE_DATA = window.SITE_DATA || null;
